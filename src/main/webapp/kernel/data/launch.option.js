export default class LaunchOption {
    /**
     * @param {String} name
     * @param {Number} width
     * @param {Number} height
     * @param {Number} x
     * @param {Number} y
     */
    constructor(name, width, height, x, y) {
        this.name = name;
        this.width = width;
        this.height = height;
        this.x = x;
        this.y = y;
        // other thing here
    }
}
