export default class LaunchOption {
    /**
     * @param {String} name
     * @param {Number} width
     * @param {Number} height
     */
    constructor(name, width, height) {
        this.name = name;
        this.width = width;
        this.height = height;
        // other thing here
    }
}
